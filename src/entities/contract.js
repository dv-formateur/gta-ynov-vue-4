export default class contract{
    constructor(id, userId, dateStart, dateEnd, role){
        this.id = id;
        this.userId = userId;
        this.dateStart = dateStart;
        this.dateEnd = dateEnd;
        this.role = role;
    }
}